# Secrets — never commit
.env
.env.local
.env.*.local
*.pem
*.key

# Windows noise
Thumbs.db
Desktop.ini
ehthumbs.db
$RECYCLE.BIN/

# macOS noise
.DS_Store
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node (only if you add tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Python (the scripts/ folder)
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# Local backups / scratch
backups/
*.bak
*.orig
tmp/

# Vercel local dev
.vercel

# Supabase CLI local state (fine to keep config.toml but not generated state)
supabase/.temp/
supabase/.branches/
